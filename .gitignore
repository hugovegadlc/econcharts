# Proprietary source material — do NOT publish
bbva source/
macro source/
*.xlam

# Build / ship artifacts (regenerable). ship/econcharts.spec + ship/build.py +
# ship/launch.py are checked in (the bundle manifest) — only outputs are ignored.
ship/venv/
ship/build/
ship/dist/
ship/buildlog.txt
mock_ship/build/
mock_ship/dist/
mock_ship/venv/

# Rendered outputs (regenerable; goldens in tests/baseline/ are NOT ignored)
examples/_gallery/
examples/*.png
figuras/
*.pptx

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.eggs/
build/
dist/

# Editor / OS
.DS_Store
Thumbs.db
